// src/components/ReactComponent.tsx
import React from 'react';

const ReactComponent: React.FC = () => {
  return (
    <div>
      <h2>This is a React component</h2>
      <p>It's being used in an Astro page!</p>
    </div>
  );
};

export default ReactComponent;